/**
 * The package proxy allows to make certain points and
 * when and where it should be rendered
 * 
 * <p>
 * Server proxy is less common use than the client proxy.
 * The common proxy utilizes both server and client.
 * 
 */
package com.mreyeballs29.issactncore.proxy;